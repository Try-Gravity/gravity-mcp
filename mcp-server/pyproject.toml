[project]
name = "gravity-mcp-server"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastmcp>=3.0"]

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
